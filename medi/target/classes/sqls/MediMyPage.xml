<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="MediMyPage">

<select id="mydetail"
parameterType="kh.com.medi.model.MediMyPageDto"
resultType="kh.com.medi.model.MediMyPageDto">
	SELECT SEQ, ID, PWD, NAME, EMAIL, PHONE, 
	POST, ADDRESS, AUTH, DEL, MYIMG, BLACKLIST,REGDATE,QUESTION,ANSWER
	FROM medi_member
	WHERE ID=#{id}
</select>

<update id="myupdate" parameterType="hashmap">
UPDATE medi_member SET 
PWD=#{pwd}, EMAIL=#{email},POST=#{post},ADDRESS=#{address}
WHERE SEQ=#{seq}
</update>

<update id="mydelete" parameterType="kh.com.medi.model.MediMyPageDto">
	UPDATE medi_member
	SET DEL=1
	WHERE ID=#{id}
</update>


</mapper>